to run this code you will need to:

- set a .env file like the .env.example file
  ```plaintext
  DB_USER=postgres
  DB_PASSWORD=postgres
  DB_HOST=localhost
  DB_PORT=5432
  DB_NAME=postgres
  PORT=3000
  ```
